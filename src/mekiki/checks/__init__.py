"""Data-quality checks over ``Episode``/``Frame`` objects (see
``mekiki.episode``). Each check reports a magnitude and the threshold it
was compared against — never a bare pass/fail — so a user with different
tolerances than mekiki's defaults can still make sense of the result.
"""
