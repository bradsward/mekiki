# State

no memory between sessions — this file plus ROADMAP.md and DECISIONS.md is it.

## where things are

M0's done. skeleton's in: pyproject (hatchling, ruff, mypy strict, pytest config), `src/mekiki` with a stub CLI (`--version` only, real subcommands land in M5), tests passing, CI matrix on 3.10-3.13, pre-commit config, apache-2.0 license, readme.

local venv at `.venv/` (gitignored) — ruff, mypy, pytest, pytest-cov, numpy, pyarrow, installed via `pip install -e ".[dev]"`. no uv on this machine, just venv + pip. reuse `.venv/Scripts/python.exe -m <tool>` rather than recreating it unless it's actually missing.

repo lives at github.com/bradsward/mekiki, public since 2026-08-31. ruff check, ruff format --check, mypy --strict, pytest -q all pass, coverage 99% (bar's 90%).

## next

`docs/episode.md` and the `Episode`/`Frame` dataclasses (`src/mekiki/episode.py`) are both done — `ActionDimSpec`/`ActionSpaceSpec`, `Pose`, `Proprioception`, `CameraFrame` (lazy `read()`, never decoded at construction), `Frame`, `EpisodeMetadata`, `Episode` (iterable once, per the streaming rule). Shape/range validation lives in `__post_init__` on `Pose` and `Proprioception`. `tests/conftest.py` has a `make_clean_episode`/`make_clean_frame` factory meant to be reused as the "clean control" fixture once actual detectors (M2+) need one alongside their defect-injected cases.

`Proprioception` got revised same-day: `ee_poses`/`grippers` are now `dict[str, ...]` keyed by end-effector name (so zero, one, or two — bimanual — all work) instead of one required `ee_pose`/`gripper`, `joint_positions` is optional, and there's an `extra: dict[str, NDArray]` escape hatch for state that isn't joint/pose/gripper shaped (PushT's raw 2D position lives there). Reasoning in DECISIONS.md — the deciding factor was that even ALOHA-style bimanual arms broke the old single-`ee_pose` shape, not just non-arm tasks.

`src/mekiki/readers/lerobot.py` now reads a LeRobotDataset directory end to end, across **both** on-disk layouts the format actually uses in the wild — not just v3.0. State columns (e.g. `observation.state`) are never guessed into joints/ee/gripper — they land in `Proprioception.extra`, keyed by their LeRobotDataset column name, same "don't guess" principle as the action space. Verified against three real datasets (not committed — network fixtures outside the repo):

- v3.0, `lerobot/pusht`: 206/206 episodes, 25650/25650 frames match `info.total_frames`.
- v2.0, `IPEC-COMMUNITY/bridge_orig_lerobot` (this project's own README's `bridge_v2` example): all 53192/53192 episodes and 1893026/1893026 frames match, first two episodes' actual frame data spot-checked by hand (actions, 4 camera keys, language instructions, `robot_embodiment` correctly picked up as `"widowx"` from the dataset's own `robot_type` rather than defaulting to `"unknown"`).
- v2.1, `IPEC-COMMUNITY/berkeley_cable_routing_lerobot`: confirmed layout-compatible with v2.0 for what this reader touches.

decided to do v2.x first (done above), keep native RLDS on the roadmap but after it — reasoning and the reader-internals design tradeoffs are in DECISIONS.md.

camera pixels are still the one thing not real across either layout: `Frame.images` correctly reports that a camera exists (name, resolution) but `.read()` raises `NotImplementedError` — real cameras are video-encoded (av1) and mekiki has no decoder yet. Still deliberately deferred; see parked.

M2 (temporal integrity) started: split it into four sub-checks in `ROADMAP.md` (non-monotonic timestamps, control frequency jitter, dropped frames, camera/proprio desync) since it wasn't broken down before. First one done: `src/mekiki/checks/temporal.py`'s `check_timestamp_monotonicity` — streams an episode once (doesn't materialize all frames), reports `min_delta_seconds` against a `threshold_seconds` plus which frame indices violated it, never a bare pass/fail. Tested against both a clean control (`make_clean_episode`) and precisely-injected defects (duplicate timestamp → 0.0 delta, out-of-order → negative delta, sub-threshold gap only caught with a stricter threshold), plus checked against a real `lerobot/pusht` episode (161 frames, 0 violations, ~0.1s deltas matching its 10Hz rate).

Second one done: `check_control_frequency_jitter`. Takes `nominal_hz` as a required, caller-supplied value (the dataset's own declared fps, e.g. `LeRobotInfo.fps`) rather than inferring a nominal rate from the data itself — inferring it (median delta or similar) can never catch a systematic wrong-rate error, since the episode would just look self-consistent against its own median. Threshold is a fraction of the nominal interval (default 20%), not a fixed number of seconds, since the same absolute jitter means very different things at 5Hz vs 100Hz. Tested against a clean control, a single large delay at a known magnitude, a small in-threshold wobble, a tightened threshold catching that same wobble, and — the one that actually matters — a deliberately wrong declared rate (supplying 2x the dataset's real fps) flagging every single frame, proving the "declared, not inferred" design choice actually does what it's for. Verified against the real `lerobot/pusht` episode too: correct fps (10.0) gives ~0 jitter (floating-point noise only), doubled fps flags all 160 pairs.

Third one done: `check_dropped_frames`. Same declared-`nominal_hz` approach as jitter, but a different question: not "is this delta noisy" but "is this delta big enough that a whole frame is more plausibly missing than just late" (default: 1.5x the nominal interval). Estimates how many frames are missing per gap (`round(delta / nominal_dt) - 1`), not just that a gap exists. Caught a real bug while testing it: two test functions in `test_temporal.py` accidentally shared the exact same name (`test_rejects_non_positive_nominal_hz`, one for jitter's guard and one for dropped-frames'), so the second silently shadowed the first at import time and the jitter check's own input validation was never actually being tested — the coverage report's one missing line gave it away. Renamed both to be unique; would've stayed invisible otherwise since pytest doesn't warn on duplicate names within a file, it just silently keeps the last one. Verified against real `lerobot/pusht`: clean, 0 estimated dropped frames.

Fourth and last one done: `check_camera_proprio_desync`. Returns a dict keyed by camera name (a multi-camera episode gets independent results per camera, never averaged together). The real point of this one is the `is_measured` flag on each result — `CameraFrame.timestamp_is_measured` is `False` whenever a reader assumed a camera synced to the control loop rather than actually measuring it (which, per docs/episode.md, is what `mekiki.readers.lerobot` always does right now — LeRobotDataset doesn't record per-camera timestamps separately). `n_measured_frames == 0` must never be read as "0 seconds of desync" — it means desync was never actually checked. Tested that an unmeasured frame with a deliberately huge raw timestamp gap doesn't leak into the measured stats at all. Verified against real data on both sides of the reader: `lerobot/pusht`'s one camera and all 4 of `bridge_orig_lerobot`'s cameras correctly report `is_measured=False` — the honest answer, since neither dataset's reader has anything measured to report yet.

M2 (temporal integrity) is done — all four sub-checks in `mekiki.checks.temporal`, each with a clean control, precisely-injected defects at known magnitudes, and real-data verification.

`docs/consistency.md` is now written — M3's design doc, same discipline `docs/episode.md` got before any Episode/Frame code. The load-bearing decision in it: `ActionDimSpec` never says what physical quantity an action dimension predicts (position axis? orientation? gripper? joint?), and forward integration needs exactly that, so M3 introduces a new, separate, caller-supplied `ActionTargetSpec` rather than extending the already-shipped M1 types. Also pins down: single-step integration only (multi-step rollout is a different, harder problem, explicitly out of scope), frame handling limited to "same frame" and "delta in the ee frame, rotated by current orientation" (both computable from data mekiki already has — anything else fails loudly per dimension), quaternion composition for orientation deltas (not axis-wise addition — orientation isn't Euclidean), and a `base_tolerance + rate_tolerance_per_second * dt` tolerance model where both coefficients are caller-supplied, never invented by mekiki (same "declared, not inferred" principle as `nominal_hz` in M2).

`ActionTarget`/`ActionTargetSpec` are now real code: `mekiki.checks.consistency` — `ActionTarget` (kind + end_effector/axis/joint_index depending on kind) and `validate_action_target_spec`, which enforces the one structural rule the design doc calls out: every `position_axis`/`orientation_axis` group (same kind + end_effector) must have all three of x/y/z present exactly once, since partial or duplicated axes can't be integrated as a vector. Tested against complete specs (single end-effector, bimanual — two independent groups, joints, orientation), the all-`None` degenerate case (nothing modeled, still valid), and each specific way a spec can be incomplete (missing axis named exactly in the error, duplicate axis, missing end_effector/joint_index, length mismatch). No forward-integration math yet — this is validation only, exactly the next unblocked slice per the roadmap's own breakdown.

The actual integration math is done: `predict_next_proprioception` in `mekiki.checks.consistency`, implementing every per-target-kind rule from the design doc — position (absolute needs an exact frame match, delta supports same-frame and the ee-frame rotation-by-current-orientation case), orientation (delta only, ee-frame only, composed via quaternion multiply — real Hamilton-product/exponential-map math, not axis-wise addition), gripper (absolute + normalized unit only), joint (direct or delta, no frame concept). Also tightened `validate_action_target_spec` to require a group's three axes share the same mode/frame/unit, not just be structurally complete — a real gap the previous session's validation missed. Full reasoning for both this and the orientation-frame scope narrowing (below) is in `DECISIONS.md`.

The quaternion math is hand-verified, not just "looks plausible": rotating (1,0,0) by a 90°-about-z quaternion gives (0,1,0) exactly, composing two 90°-about-z rotations gives 180° exactly ((0,0,1,0)) — both checked against hand-computed values in `tests/checks/test_consistency.py`, and the orientation-integration test reuses that same verified 90°+90°=180° case end to end. No real-data check yet for this piece specifically — that needs a real dataset with a *confirmed* delta/ee-frame action encoding to be meaningful, which is really the next task's job (the residual check, run against a real dataset's genuinely-clean episodes, where a small residual is the actual correct answer) rather than something to force artificially here.

The residual + tolerance check is done: `check_action_state_consistency` + `ToleranceModel`. Streams an episode once, forward-integrates each step via `predict_next_proprioception`, compares against what the next frame actually recorded — Euclidean distance for position, quaternion angular distance for orientation, absolute difference for gripper/joint — and reports each against `ToleranceModel`'s `base + rate*dt` per quantity. Results are a dict keyed `"position:<ee>"` / `"orientation:<ee>"` / `"gripper:<ee>"` / `"joint:<index>"`, one `ConsistencyResult` per quantity actually modeled, never averaged across quantities. Tested against a perfectly-consistent synthetic episode (0 residual everywhere), known-magnitude injected mismatches for all four quantity kinds, a case proving the `rate*dt` term actually loosens tolerance for a larger timestep (not just base), multi-step index tracking, and every "expected value missing from the next frame" raise path. 134 tests, 100% coverage on `mekiki.checks.consistency`.

**Real gap found while trying to verify this against actual data, not glossed over:** there is currently no way to run this check against anything `mekiki.readers.lerobot` produces. That reader deliberately never interprets a dataset's raw non-standard columns (e.g. `observation.state`) into `Proprioception.ee_poses`/`joint_positions`/`grippers` — everything lands in `extra` instead, per the same "don't guess" principle that made `ActionTargetSpec` necessary in the first place. So every real episode read so far has empty `ee_poses`, empty `grippers`, `joint_positions=None` — nothing for M3 to compute a position/orientation/gripper/joint residual against at all. M3 is fully built and rigorously tested against synthetic data and hand-verified math, but has zero path to real reader output right now. See the recommendation below — this needs a real decision, not a quick fix, and I didn't want to either fake a real-data check by guessing a dataset's orientation encoding (exactly the kind of guess this project refuses to make) or quietly leave this unmentioned.

## open questions / risks

- numpy is pinned to `<2.4` (see DECISIONS.md) purely because of a mypy/stub incompatibility — not a real dependency conflict. Remember to reconsider that pin once mypy catches up, so it doesn't quietly linger for years.

## parked

ideas noticed in passing, outside whatever the session's actual task was. not deciding these mid-task — come back and actually think them through before acting, prune once resolved so this doesn't pile up.

- **Camera frames in real LeRobotDataset entries are video-encoded** (av1-in-mp4 in every dataset checked so far), not raw arrays in parquet. Decoding needs a real dependency (something like PyAV) that isn't installed yet. Not blocking anything else in M1 (metadata/state/action all work without it) — a deliberate addition whenever a check first needs actual pixels, not something to reach for casually.
- **RLDS-native decoding needs `tensorflow`/`tensorflow_datasets` — a dependency in the same weight class as the torch dependency this project already refuses to make core.** Full findings in `docs/rlds.md`: the format is TFRecord-wrapped TFDS serialization, nested/sequence-encoded in a way that isn't independently documented — reading it correctly without TF itself means hand-rolling a decoder with nothing to validate it against, which is a bad idea for a tool whose whole job is catching silent data corruption. Priority's settled (v2.x support went first) but not the dependency question — if/when RLDS-native gets picked up, the sane path is a `mekiki[rlds]` extra (mirroring `mekiki[coverage]`) that pulls in `tensorflow_datasets`, never core. Still not sure it's worth it at all given how much OXE data the LeRobotDataset mirrors already cover.
- **Orientation-delta targets only support the `ee` (body) frame right now — same-frame (e.g. `base_link`) orientation deltas raise, not silently guessed.** The two cases need opposite quaternion composition order (post- vs pre-multiply), and `docs/consistency.md` only wrote down the `ee`-frame formula. Real gap, not a hypothetical — a dataset with world-frame orientation deltas would hit this immediately. Needs its own short design addition (the composition-order derivation, which is simple, just not yet reviewed) before implementing, not a mid-task guess. Full context in `DECISIONS.md`.
- **M3 has no path to real data yet, and this is the bigger of the two open items above.** `mekiki.readers.lerobot` puts every non-standard column into `Proprioception.extra` rather than guessing it's a joint/ee-pose/gripper — correct per M1's own philosophy, but it means real episodes never populate `ee_poses`/`joint_positions`/structured `grippers`, so `check_action_state_consistency` has nothing to run against on real data despite being fully built and tested. Two ways forward, and this is a real decision, not a cleanup task: (a) add a reader-side "state target spec" — caller-supplied, symmetric to `ActionTargetSpec` — that maps a dataset's raw state columns into structured `Proprioception` fields the same explicit, never-guessed way actions already work; or (b) accept that populating structured proprioception is each user's own responsibility (write a small per-dataset adapter before calling M3), and mekiki only ever promises the check itself, not a path to every dataset's raw columns. Genuinely unsure which is right — (a) is more useful out of the box but is a real new piece of reader-side scope; (b) keeps the reader simpler but means M3 stays synthetic-only until someone does that mapping by hand. Want your read on this before either happens.

## log

2026-08-25 · M0 · repo skeleton, tooling, CI, license, readme · next: docs/episode.md then Episode dataclasses + LeRobot reader (M1)
2026-08-25 · infra · pushed to github (private), nightly build session scheduled · next: M1
2026-08-25 · M1 · docs/episode.md written (action space, frames, streaming rules) · next: Episode/Frame dataclasses
2026-08-25 · M1 · Episode/Frame dataclasses + clean-episode test fixture · next: LeRobotDataset reader
2026-08-25 · M1 · LeRobotDataset info/episode-index reader + action-space gate, checked against real lerobot/pusht data · next: resolve Proprioception-shape question (see recommendations), then frame-data reading
2026-08-25 · M1 · generalized Proprioception for bimanual/no-arm state (dict-keyed ee_poses/grippers, extra escape hatch) · next: frame-data parquet reading in the LeRobotDataset reader
2026-08-26 · M1 · LeRobotDataset reader now builds real Frame/Proprioception objects, verified end-to-end against lerobot/pusht (206 episodes, 25650 frames) · next: RLDS/Open X-Embodiment reader, or camera decoding if that becomes urgent first
2026-08-26 · M1 · researched real RLDS/OXE format (docs/rlds.md), added codebase_version guard after finding real data (bridge_orig_lerobot) is LeRobotDataset v2.x not v3.0 · next: decide RLDS-native priority/dependency, then LeRobotDataset v2.x support
2026-08-26 · M1 · LeRobotDataset v2.0/v2.1 support added (decided this goes before native RLDS), verified against real bridge_orig_lerobot (53192 episodes, 1893026 frames match) and berkeley_cable_routing_lerobot (v2.1) · next: RLDS-native reader (still haven't committed to the TF dependency) or camera decoding, whichever becomes relevant first
2026-08-27 · M2 · split M2 into 4 sub-checks, implemented the first (timestamp monotonicity), verified against real lerobot/pusht data · next: control frequency jitter or dropped frames
2026-08-27 · infra · pulled the nightly scheduled task — every run since it was set up died silently at a broken sleep step and never actually did anything, so it wasn't earning its keep. everything in this repo so far is from working sessions directly, not unattended runs. revisit with a simpler design later.
2026-08-31 · docs · fixed a wrong Homepage URL, gitignored .claude/, made the CLI README examples clearly aspirational and added a real working code sample, went public on GitHub · next: control frequency jitter or dropped frames
2026-09-12 · M2 · control frequency jitter check, verified against real lerobot/pusht data (correct fps clean, doubled fps flags every frame) · next: dropped frames, then camera/proprio desync
2026-09-12 · M2 · dropped frames check, found and fixed a duplicate-test-name bug that was silently skipping the jitter check's own validation test · next: camera/proprio desync (last M2 sub-check), then M3
2026-09-12 · M2 · camera/proprio desync check, verified against real lerobot/pusht and bridge_orig_lerobot (all cameras correctly report unmeasured, not falsely "synced") · M2 is done · next: docs/consistency.md, then M3 itself
2026-09-12 · M3 · docs/consistency.md written — ActionTargetSpec correspondence (new type, not an ActionDimSpec extension), frame handling scoped to same-frame + ee-rotation, quaternion composition for orientation, caller-supplied base+rate*dt tolerance model · next: ActionTarget/ActionTargetSpec types, then the integration math
2026-09-13 · M3 · ActionTarget/ActionTargetSpec types + validate_action_target_spec implemented (mekiki.checks.consistency), position/orientation triple-completeness enforced, tested including bimanual and all-None degenerate cases · next: forward-integration math per target kind
2026-09-13 · M3 · predict_next_proprioception implemented — position/orientation/gripper/joint integration, quaternion composition hand-verified, tightened group mode/frame/unit validation, scoped orientation deltas to ee-frame only (parked: same-frame composition order) · next: residual + tolerance check, first real end-to-end validation
2026-09-13 · M3 · check_action_state_consistency + ToleranceModel implemented (residuals in meters/radians/normalized fraction vs. caller-supplied base+rate*dt tolerance), 134 tests, 100% coverage · found M3 has no real-data path yet since the reader never populates structured proprioception — needs your call, see recommendations · next: decide the state-correspondence question, then M3 is genuinely done
